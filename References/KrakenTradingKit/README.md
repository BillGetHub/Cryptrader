# KrakenTradingKit

This was previously tracked as a broken git submodule reference (a gitlink with no
`.gitmodules` entry, so it never resolved to any actual content). The original source
repo is no longer known.

If you find or recall the source, add it back as a proper submodule:

```bash
git submodule add <repo-url> References/KrakenTradingKit
```

Until then, this is a placeholder — no content to reference here.
