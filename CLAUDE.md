# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty scaffolding: it contains only `README.md` (just the project title, "Cryptrader"). There is no source code, build system, package manifest, linter, or test suite yet.

There are no commands to build, lint, or test at this time — none of that tooling exists in the repo. When code is added, update this file with the actual commands (build/lint/test/single-test invocations) and a description of the architecture, based on what is actually present rather than assumptions about what a "crypto trader" project typically contains.
